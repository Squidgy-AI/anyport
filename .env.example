# TokenRouter — get from tokenrouter.com after redeeming hackathon $200 credit
TOKENROUTER_API_KEY=
TOKENROUTER_BASE_URL=https://api.tokenrouter.com/v1

# Reboot.dev — get from workshop at 12:45
REBOOT_API_KEY=
REBOOT_BASE_URL=https://api.reboot.dev

# Supabase — create a free project at supabase.com (or reuse Squidgy dev)
NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Public host (for callbacks / install URLs)
NEXT_PUBLIC_APP_URL=http://localhost:3000
